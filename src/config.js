// ============================================================
//  BG INDIA PORTAL — CONFIG LOADER
//  Real per-host values live in hosts/<hostId>/config.js.
//  Host is selected at build time via VITE_HOST (see vite.*.config.js'
//  '@host-config' alias); defaults to 'dwarka' if unset.
//  To onboard another host: add hosts/<hostId>/config.js, don't edit here.
// ============================================================

export { CONFIG } from '@host-config'
